# RISKS.md

| ID   | Risk                                                                                           | Likelihood | Impact | Mitigation in place / planned                                                                     | Owner     |
| ---- | ---------------------------------------------------------------------------------------------- | ---------- | ------ | ------------------------------------------------------------------------------------------------- | --------- |
| R-01 | A cached figure shown on a money screen leads to a wrong remittance.                           | Medium     | High   | P-03: `'use cache'` is banned in `(app)`, `(portal)`, `(resident)`. Lint rule planned Sprint 002. | web       |
| R-02 | Cross-organization data leak through a missed `organization_id` filter.                        | Medium     | High   | RLS forced on every tenant table; chassis sets scope per transaction; E3 harness per service.     | platform  |
| R-03 | Float arithmetic on rent or fees.                                                              | Medium     | High   | `bigint` minor units; ESLint bans `number` in money files.                                        | money     |
| R-04 | Webhook retries from M-Pesa/WhatsApp cause duplicate postings.                                 | High       | High   | Gateway inbox with idempotency keys; postings keyed by receipt number.                            | gateway   |
| R-05 | Next.js CVE unpatched in production.                                                           | Medium     | High   | Renovate high-priority group, 7-day window; Trivy blocks HIGH/CRITICAL in CI.                     | platform  |
| R-06 | Landing copy drifts into fabricated claims (fees, tiers, "verified", partner names).           | Medium     | Medium | All copy in `content/landing.ts`; QUESTIONS.md assumptions; review checklist in acceptance.       | product   |
| R-07 | `cacheComponents` build errors when a Server Component touches a request API outside Suspense. | Medium     | Low    | Landing calls no request API; results page reads `searchParams` inside a Suspense boundary.       | web       |
| R-08 | Docker image not built in the Sprint 001 environment (no daemon).                              | Certain    | Low    | Dockerfile written against `output: 'standalone'`; CI `image-scan` job builds and scans it.       | platform  |
| R-09 | Design-system drift between marketing and dashboard.                                           | Medium     | Medium | Tokens only in `packages/ui/theme.css`; Fraunces restricted to marketing headings.                | ui        |
| R-10 | WhatsApp Business API template approval delays block launch messaging.                         | Medium     | High   | Start template submission in Sprint 002; SMS fallback decision queued (Q-18 family).              | messaging |
